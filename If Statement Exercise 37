number_of_sides = int(input())

if number_of_sides == 3:
    print ("The shape is a triangle. There are many different types of triangles. Some types are based off of the angles of the triangles, such as acute triangles, obtuse triangles and right triangles. Other triangles are based off the length of their sides, such as equilateral triangles, isosceles triangles, and scalene triangles.")

if number_of_sides == 4:
    print ("The shape is a quadilateral. There are many different quadilateral shapes such as squares, rectangles, parallelograms, rhombuses, kites, and trapezoids.")

if number_of_sides == 5:
    print ("The shape is a pentagon.")

if number_of_sides == 6:
    print("The shape is a hexagon.")

if number_of_sides == 7:
    print("The shape is a heptagon.")

if number_of_sides == 8:
    print("The shape is an octagon.")

if number_of_sides == 9:
    print("The shape is a nonagon.")

if number_of_sides == 10:
    print("The shape is a decagon.")
